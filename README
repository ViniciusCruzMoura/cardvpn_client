This repository contains a simple C program for a VPN client called `cardvpn_client`. 
The program is designed to work with OpenConnect.

$ Requirements
- OpenConnect must be installed on your system.

$ Compilation
To compile the code, navigate to the repository directory 
and run the following command:

    $ make

This will generate an executable file named `cardvpn_client`.

$ Usage
After compiling, you can run the program using:

    $ ./cardvpn_client
